#include <stdio.h>
#include <stdlib.h>

int main()
{
    char a = 'B',b = 'O',c = 'Y';
    /**
     * 使用一个一个字符独立输出
     * */
    putchar(a); 
    putchar(b);
    putchar(c);
    putchar('\n');
    system("pause");
    return 0;
}