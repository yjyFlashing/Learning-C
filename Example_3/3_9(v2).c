#include <stdio.h>
#include <stdlib.h>

int main()
{
    char a,b,c;
    /**
     * 直接输出键盘所输入的字符
     * */
    putchar(getchar());
    putchar(getchar());
    putchar(getchar());
    putchar('\n');
    system("pause");
    return 0;
}